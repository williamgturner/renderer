#include "raycast.h"
#include "gamespace.h"
#include "player.h"

void raycastLoop(GameSpace *game) {
  player *cam = &game->camera;
}
/*
 * Increments 4-vector by its magnitude until it intersects with a wall
 * @param 4-vector
 * @param integer array representing game-space
*/
vec4 marchRay(vec4 v1, int map[]) {
  return v1;
}
